import Triage from "@/components/Triage";

// 破壁 GlobalInfo — daily-tracking screen (Option C · 阅读 Triage).
// Three-pane inbox/reader on desktop; list → reader push flow on mobile.
export default function Home() {
  return <Triage />;
}
